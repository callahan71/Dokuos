{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
		<div class="col-md-10">
			<h1>Editar video</h1>
		</div>
	</div>

    {{ form_start(edit_form, {'attr': {'class': 'form-horizontal'} }) }}
        <hr>
        <div class="form-group">
			<label class="col-md-4 control-label">Video</label>
			<div class="col-md-5">
			  {{ form_widget(edit_form.video, {'attr': {'class': 'form-control'}}) }}
			</div>
		</div>
            
        {% if is_granted('ROLE_ADMIN') %}
        <div class="form-group">
			<label class="col-md-4 control-label">Usuario</label>
			<div class="col-md-5">
			  {{ form_widget(edit_form.userid, {'attr': {'class': 'form-control'}}) }}
			</div>
		</div>
        {% endif %}
        
        <hr>
        <div class="form-group">
			<div class="col-md-offset-5 col-md-1">
			  <button type="submit" class="btn btn-primary">Editar</button>
			</div>

			<div class="col-md-1">
			  <a class="btn btn-default" href="{{ path('show_video_index') }}" role="button">Volver</a>
			</div>
		</div>
    {{ form_end(edit_form) }}

{% endblock %}
